import 'package:flutter/material.dart';

class AudioListCard extends StatelessWidget {
  const AudioListCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
